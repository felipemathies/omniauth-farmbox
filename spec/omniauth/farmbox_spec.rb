require "spec_helper"

RSpec.describe Omniauth::Farmbox do
  it "has a version number" do
    expect(Omniauth::Farmbox::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
